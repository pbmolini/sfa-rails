<%= simple_form_for([@boat, @booking]) do |f| %>
  <% if @booking.errors[:base].any? %>
    <p class="error"><%= @booking.errors[:base] %></p>
  <% end %>

  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :start_time %>
    <%= f.input :end_time %>
    <%= f.input :people_on_board %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
